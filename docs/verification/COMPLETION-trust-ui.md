# Paged inbox interpretation and owner isolation verification

Date: 2026-09-13. This is local synthetic browser evidence, not human or live-provider acceptance.

The tracked `scripts/run-intelligence-trust-ui.js` launcher ran `scripts/verify-intelligence-trust-ui.js` against an owned ephemeral loopback server and disposable in-memory SQLite. Chromium 140.0.7339.16 was loaded from the existing installed Playwright module; no dependency was installed. The real factory gateway recorded seven injected synthetic model admissions. No live provider was contacted.

Command:

```powershell
node scripts/run-intelligence-trust-ui.js --playwright-module C:/Users/ghg/anaconda3/Lib/site-packages/playwright/driver/package/index.mjs
```

Result: **20 / 20 checks passed**. The actual requested production assets were `index-DQvPnJVA.js`, `jsx-runtime-B-hcVAMW.js`, and `index-Bw18lWd4.css`; the verifier asserted that `client/dist/index.html` remained unchanged during the run.

All original nineteen interpretation checks remain. Existing saved extraction, exact source attribution, model selection, empty/rejected/unavailable fallback, historical missing methods, research confidence limitations, original reply text, unknown replies, unconfirmed candidates, explicit stop, low-confidence possible opt-out, missing original text, detail consistency, read-only interaction and absence of runtime errors were exercised through the new paged inbox. Twenty-two extra synthetic enquiries force directory pagination. Directory rows are metadata only; interpretation and original-text assertions open the actual selected message panel. The review link now opens the existing reminders/outcomes lead view, from which intelligence remains reachable.

The twentieth check expires the current session, signs in as a different owner within the same SPA document, and deliberately delays the new owner's inbox GET. Previously cached lead names must be absent before that read returns. The returned new workspace is empty. The sole browser POST in that additional case is the explicit login; the original no-automatic-analysis/sending assertion remains unchanged.

Initial verifier runs exposed selector assumptions about the select's accessible name, retained page state on same-route navigation, and cached pages making no redundant GET. These were aligned with the shipped behavior. No interpretation assertion was removed or relaxed. The final paging check explicitly refreshes the selected page to verify its actual GET.

Synthetic screenshots are in `C:\Users\ghg\AppData\Local\Temp\relay-intelligence-trust-ui-oI4K50`. Artifacts are temporary and are not committed. The owned browser/server were closed. Real operator inbox usability, screen-reader review and live-provider interpretation quality remain separate acceptance work.
